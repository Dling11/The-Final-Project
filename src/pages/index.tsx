import Head from "next/head";
import { FC, useState } from "react";
import { SelectedPage } from "~/mylinks/types";
import Navbar from "~/pages/a-navbar";
import Header from "~/pages/c-header";
import About from "./d-aboutme/about";
import Progress from "./e-progress/Progress";

const Home: FC & { isPublic?: boolean } = () => {

  const [selectedPage, setSelectedPage] = useState<SelectedPage>(SelectedPage.Home);

  return (
    <>
      <Head>
        <title>Rowell Is A God</title>
        <meta name="Portfolio" content="Generated By God's" />
        <link rel="icon" href="/Bart1.png" />
      </Head>
      
      {/*=======================> components <======================== */}
      <Navbar 
        selectedPage={selectedPage}
        setSelectedPage={setSelectedPage}
      />
      <Header 
        selectedPage={selectedPage}
        setSelectedPage={setSelectedPage}
      />
      <About 
        selectedPage={selectedPage}
        setSelectedPage={setSelectedPage}
      />
      <Progress 
        selectedPage={selectedPage}
        setSelectedPage={setSelectedPage}
      />
    </>
  );
};

Home.isPublic = true;

export default Home;
